name: Update profile status

on:
  push:
    branches: [main]
  schedule:
    - cron: '17 */3 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-status:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/update-status.mjs
      - name: Commit status update
        run: |
          git config user.name "profile-status-bot"
          git config user.email "profile-status-bot@users.noreply.github.com"
          git add README.md
          git diff --cached --quiet || git commit -m "chore: update profile status"
          git push
